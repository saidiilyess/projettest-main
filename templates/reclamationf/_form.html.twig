{# reclamationf/_form.html.twig #}

<form action="{{ path('app_reclamationf_new') }}" method="post" id="newreclamationfForm" novalidate>
	{{ form_start(form) }}

	<div>
    <label for="{{ form.date_rec.vars.id }}">Date de réclamation:</label>
    {{ form_widget(form.date_rec, {'attr': {'required': 'required', 'pattern': '\d{4}-\d{2}-\d{2}'}}) }}
    <small>Format: YYYY-MM-DD</small>
</div>


	<div>
		<label for="{{ form.description.vars.id }}">description:</label>
		{{ form_widget(form.description, {'attr': {'required': 'required', 'pattern': '[A-Za-z]+'}}) }}
		<small>select the correct cours</small>
	</div>
	<div>
		<label for="{{ form.type.vars.id }}">type:</label>
		{{ form_widget(form.type, {'attr': {'pattern': '[A-Za-z]+'}}) }}
		<small>select the correct user</small>
	</div>
	<button type="submit" class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</form>

<script>
	// Add an event listener for form submission
document.getElementById('newreclamationfForm').addEventListener('submit', function (event) { // Check form validity before submission
if (!this.checkValidity()) {
event.preventDefault(); // Prevent form submission if not valid
alert('Please fill in all required fields with valid input.');
}
});
</script>

