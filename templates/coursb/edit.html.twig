{% extends 'baseB.html.twig' %}

{% block body %}
	<h1>Edit Cours</h1>

	{{ include('coursb/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_coursb_index') }}">back to list</a>

	{{ include('coursb/_delete_form.html.twig') }}
{% endblock %}
